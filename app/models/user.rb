class User < ActiveRecord::Base
  def mint_update
    # Get mint info
    # Find or create the account, making sure it's current
    # Create a balance update for that account
    # Iterate through the rest of the current accounts
  end
end
